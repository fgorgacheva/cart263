"use strict";

let animalChoice;
let moneyBalance;
let gameState;

// setup()
//
function setup() {
    createCanvas(windowWidth, windowHeight);

    moneyBalance = 0;

    gameState = "beginning";

}